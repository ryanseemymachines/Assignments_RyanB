* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    margin: 0;
    padding: 0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
        'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
        sans-serif;
}


import HomeHeader from '../../components/HomeHeader';
import HomeFooter from '../../components/HomeFooter';
import PostList from '../../components/PostList';
import styles from './index.module.css';

function Home() {
  return (
    <div className={styles.pageWrapper}>
      <HomeHeader/>
      <div className={styles.contentWrapper}>
        <PostList/>
      </div>
      <HomeFooter/>
    </div>
  )
}

export default Home;




.pageWrapper {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

.contentWrapper {
    flex: 1;
}

.textAreaInput{
    width: 100%;
    padding: 12px;
    font-size: 1.2em;
    border: 2px solid lightgrey;
    border-radius: 8px;
    resize: vertical;
    box-sizing: border-box;
    min-height: 150px;
}

.textAreaInput:focus{
    box-shadow: 0 0 20px rgb(223, 223, 223);
    outline: none;
    border: 2px solid rgb(123, 123, 123);
}

import { useState } from "react";
import BlogPost from "../BlogPost";
import BlogHeader from "../BlogHeader";
import CreateBlogButton from "../CreateBlogButton";
import NewPostForm from "../NewPostForm";
import Modal from "../Modal";

function PostList() {
  const [posts, setPosts] = useState([
   {
      id: 1,
      title: "React Basics",
      author: "Jane Doe",
      date: "18-07-2024",
      content: "React is a popular JavaScript library developed by Facebook for building user interfaces, especially single-page applications (SPAs). It helps developers create fast and interactive web apps by breaking the UI into reusable components. Instead of reloading an entire page every time something changes, React updates only the parts of the page that need to change — making it efficient and responsive.\n\nThe core idea of React revolves around components. A component is a small, reusable piece of UI — for example, a button, header, or form. Components can be either class components or functional components, though functional components are now preferred with React Hooks. React uses a syntax called JSX (JavaScript XML), which allows you to write HTML-like code inside JavaScript. JSX makes it easier to visualize and structure UI elements directly within the logic.",
    },
    {
      id: 2,
      title: "State and Props",
      author: "John Smith",
      date: "19-07-2024",
      content: "React components often need to share or manage data. This is where props and state come in. Props (properties) are used to pass data from a parent component to a child component — similar to function parameters. State, on the other hand, represents data that belongs to a component itself and can change over time. When a component's state changes, React automatically re-renders that part of the UI to reflect the new data.",
    },
    {
      id: 3,
      title: "Lifecycle Methods",
      author: "Emily Johnson",
      date: "20-07-2024",
      content: "In React, lifecycle methods are special functions that run automatically at different stages of a component's life — from when it is created (mounted) to when it is removed (unmounted) from the UI. They allow developers to perform specific actions at these stages, such as fetching data, updating the DOM, or cleaning up resources. Lifecycle methods are mainly used in class components, while React Hooks serve a similar purpose in functional components.",
    },
  ]);

  const [selectedId,setSelectedId] = useState(posts[0].id);

  const [isModalOpen, setIsModalOpen] = useState(false);

  const displayPost=posts.find(post => post.id===selectedId);

  const handleAddPost = (newPost) => {
    setPosts((prev) => [newPost, ...prev]);
    setSelectedId(newPost.id);
  };

  return (
    <div>
      <section >
        <BlogHeader posts={posts} selectedPost={setSelectedId} selectedId={selectedId}/>
        <CreateBlogButton
          type="button"
          title="+ Create New Blog"
          onClick={() => setIsModalOpen(true)}
        />
        <Modal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)}>
          <NewPostForm
            onAddPost={handleAddPost}
            onClose={() => setIsModalOpen(false)}
          />
        </Modal>
        
        {displayPost &&
          <BlogPost
            key={displayPost.id}
            title={displayPost.title}
            author={displayPost.author}
            date={displayPost.date}
            content={displayPost.content}
          />}

      </section>
    </div>
  );
}

export default PostList;


import styles from "./index.module.css";
import { useState } from "react";
import Input from "../Input";
import TextArea from "../TextArea";
import AddPostButton from "../AddPostButton";
import CloseButton from "../CloseButton";

function NewPostForm({ onAddPost, onClose }) {
  const [newPost, setNewPost] = useState({
    title: "",
    author: "",
    content: "",
  });

  const handleChange = (e) => {
    const { name, value } = e.target;
    setNewPost((prev) => ({ ...prev, [name]: value }));
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (!newPost.title || !newPost.author || !newPost.content) {
      alert("Please fill out all fields.");
      return;
    } 
      onAddPost({
        id: Date.now(),
        title: newPost.title,
        author: newPost.author,
        date: new Date().toLocaleDateString(),
        content: newPost.content,
      });
    
    setNewPost({ title: "", author: "", content: "" });
    onClose();
  };

  return (
    <form className={styles.formContainer} onSubmit={handleSubmit}>
      <div className={styles.formGrp}>
        <div className={styles.titleGrp}>
          <h2>Add new Blog Post</h2>
          <CloseButton type="button" onClick={onClose} />
        </div>

        <Input
          type="text"
          name="title"
          placeholder="Post Title"
          value={newPost.title}
          onChange={handleChange}
        />

        <Input
          type="text"
          name="author"
          placeholder="Author"
          value={newPost.author}
          onChange={handleChange}
        />

        <TextArea
          name="content"
          placeholder="Enter the content"
          value={newPost.content}
          onChange={handleChange}
        />

        <AddPostButton
          type="submit"
          title="Publish Post"
        />
      </div>
    </form>
  );
}

export default NewPostForm;

.formContainer{
    display: flex;
    justify-content: center;
    align-items: center;
}

.formGrp{
    display: flex;
    flex-direction: column;
    gap: 20px;
    width: 100%;
    border-radius: 16px;
}

.titleGrp{
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
}
.inputField{
    width: 100%;
    padding: 12px;
    font-size: 1.2em;
    border: 2px solid lightgrey;
    border-radius: 8px;
    transition: all 0.2s ease;
    box-sizing: border-box;
}

.inputField:focus{
    box-shadow: 0 0 20px rgb(223, 223, 223);
    outline: none;
    border: 2px solid rgb(123, 123, 123);
}


.footerGrp{
    background-color: rgb(251, 234, 214);
    display: flex;
    align-items: center;
    justify-content: space-around;
    padding: 20px;
    width: 100%;
    margin-top: auto;
}

.footerGrp p{
    margin: 10px 0 0;
}

.socialsGrp ul{
    display: flex;
    list-style: none;
    gap: 40px;
}

.socialsGrp ul li{
    cursor: pointer;
    transition: all 0.2s ease;
}

.socialsGrp ul li:hover{
    text-decoration: underline;
}


import { Component } from "react";
import styles from "./index.module.css";

class BlogPost extends Component {
  componentDidMount() {
    console.log(`BlogPost "${this.props.title}" has been mounted`);
  }

  componentDidUpdate(prevProps) {
    if (prevProps.title !== this.props.title) {
      console.log(
        `BlogPost has been updated from "${prevProps.title}" to "${this.props.title}"`
      );
    }
  }

  render() {
    const { title, author, date, content } = this.props;
    const paragraphs = content.split('\n\n').filter(p => p.trim());
    return (
      <div className={styles.blogContainer}>
        <article className={styles.blogPostCard}>
          <h2>{title}</h2>
          <p>
            <strong>By:</strong> <big>{author}</big> | <i>{date}</i>
          </p>
          {paragraphs.map((para, idx) => (
          <p key={idx} className="text-lg leading-relaxed mb-4">
            {para}
          </p>
        ))}

        </article>
      </div>
    );
  }
}

export default BlogPost;


import styles from "./index.module.css";

function BlogHeader({ posts,selectedPost , selectedId}) {
  return (
    <div className={styles.headerContainer}>
      <div className={styles.headerList} >
      {posts.map((post) => (
        <div key={post.id} className={styles.postCard} onClick={() => {selectedPost(post.id);}}>
          <h3>{post.title}</h3>
          <h5>By:{post.author} <span>{post.date}</span></h5>
        </div>
      ))}
    </div>
    </div>
    
  );
}

export default BlogHeader;


.headerContainer{
    padding: 20px;
    background-color: rgb(225, 221, 214);
    overflow-x: auto;
    overflow-y: hidden;
}

.headerContainer::-webkit-scrollbar {
    height: 8px;
}

.headerContainer::-webkit-scrollbar-track {
    background: rgb(200, 196, 189);
    border-radius: 10px;
}

.headerContainer::-webkit-scrollbar-thumb {
    background: rgb(100, 100, 100);
    border-radius: 10px;
}

.headerContainer::-webkit-scrollbar-thumb:hover {
    background: rgb(50, 50, 50);
}

.headerList{
    display: flex;
    gap: 15px;
    width: max-content; /* Important: allows content to expand beyond container */
}

.postCard{
    min-width: 200px;
    font-size: 1.1em;
    padding: 10px 50px;
    border: 1px solid black;
    border-radius: 16px;
    cursor: pointer;
    transition: all 0.2s ease;
    flex-shrink: 0; /* Prevents cards from shrinking */
}

.postCard:hover{
    transform: scale(1.02);
}

.postCard h3{
    text-decoration: underline;
}

.postCard h5{
    font-weight: 600;
}

.postCard span{
    font-weight: 300;
}


///routing

# React Router Setup Guide

## Step 1: Install React Router

npm install react-router-dom

import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';
import Contact from './pages/Contact';
import './App.css';

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>
    </Router>
  );
}

export default App;
```

## Step 3: Update HomeHeader with Navigation Links

```javascript
// components/HomeHeader/index.jsx
import { Link } from 'react-router-dom';
import styles from "./index.module.css";

function HomeHeader() {
  return (
    <header className={styles.headerContainer}>
        <Link to="/" className={styles.logoLink}>
          <h1 className={styles.logo}>BlogIt</h1>
        </Link>
        <nav className={styles.navGrp}>
          <ul>
            <li>
              <Link to="/">Home</Link>
            </li>
            <li>
              <Link to="/about">About</Link>
            </li>
            <li>
              <Link to="/contact">Contact Us</Link>
            </li>
          </ul>
        </nav>
    </header>
  );
}

export default HomeHeader;
```

## Step 4: Update HomeHeader CSS

```css
/* components/HomeHeader/index.module.css */
.headerContainer {
  background-color: rgb(252, 240, 226);
  display: flex;
  align-items: center;
  justify-content: space-around;
}

.logoLink {
  text-decoration: none;
  color: inherit;
}

.logo {
  margin: 0;
  font-size: 40px;
  cursor: pointer;
}

.navGrp ul {
  display: flex;
  gap: 30px;
}

.navGrp ul li {
  font-size: 1.2em;
  list-style: none;
  padding: 12px 15px;
  border-radius: 12px;
  transition: all 0.4s ease;
}

.navGrp ul li:hover {
  background-color: rgb(0, 0, 0);
  cursor: pointer;
  color: rgb(255, 220, 116);
}

.navGrp ul li a {
  text-decoration: none;
  color: inherit;
}
```

## Step 5: Create About Page

```javascript
// pages/About/index.jsx
import HomeHeader from '../../components/HomeHeader';
import HomeFooter from '../../components/HomeFooter';
import styles from './index.module.css';

function About() {
  return (
    <div className={styles.pageWrapper}>
      <HomeHeader/>
      <div className={styles.contentWrapper}>
        <div className={styles.aboutContainer}>
          <h1>About BlogIt</h1>
          <p>
            BlogIt is a modern blogging platform designed to make content 
            creation simple and enjoyable. Whether you're sharing technical 
            knowledge, personal stories, or creative writing, BlogIt provides 
            an intuitive interface to express your thoughts.
          </p>
          <h2>Our Mission</h2>
          <p>
            To empower writers and content creators with a clean, distraction-free 
            platform that focuses on what matters most - your content.
          </p>
        </div>
      </div>
      <HomeFooter/>
    </div>
  );
}

export default About;
```

## Step 6: Create About Page CSS

```css
/* pages/About/index.module.css */
.pageWrapper {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.contentWrapper {
  flex: 1;
  padding: 40px 20px;
}

.aboutContainer {
  max-width: 800px;
  margin: 0 auto;
  line-height: 1.8;
}

.aboutContainer h1 {
  font-size: 2.5em;
  margin-bottom: 20px;
  color: rgb(50, 50, 50);
}

.aboutContainer h2 {
  font-size: 1.8em;
  margin-top: 30px;
  margin-bottom: 15px;
  color: rgb(70, 70, 70);
}

.aboutContainer p {
  font-size: 1.2em;
  margin-bottom: 15px;
  color: rgb(80, 80, 80);
}
```

## Step 7: Create Contact Page

```javascript
// pages/Contact/index.jsx
import { useState } from 'react';
import HomeHeader from '../../components/HomeHeader';
import HomeFooter from '../../components/HomeFooter';
import Input from '../../components/Input';
import TextArea from '../../components/TextArea';
import styles from './index.module.css';

function Contact() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: ''
  });

  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({ ...prev, [name]: value }));
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (!formData.name || !formData.email || !formData.message) {
      alert('Please fill out all fields.');
      return;
    }
    alert('Message sent! We will get back to you soon.');
    setFormData({ name: '', email: '', message: '' });
  };

  return (
    <div className={styles.pageWrapper}>
      <HomeHeader/>
      <div className={styles.contentWrapper}>
        <div className={styles.contactContainer}>
          <h1>Contact Us</h1>
          <p className={styles.subtitle}>
            Have questions or feedback? We'd love to hear from you!
          </p>
          
          <div className={styles.formWrapper}>
            <Input
              type="text"
              name="name"
              placeholder="Your Name"
              value={formData.name}
              onChange={handleChange}
            />
            <Input
              type="email"
              name="email"
              placeholder="Your Email"
              value={formData.email}
              onChange={handleChange}
            />
            <TextArea
              name="message"
              placeholder="Your Message"
              value={formData.message}
              onChange={handleChange}
            />
            <button 
              className={styles.submitBtn} 
              onClick={handleSubmit}
            >
              Send Message
            </button>
          </div>
        </div>
      </div>
      <HomeFooter/>
    </div>
  );
}

export default Contact;
```

## Step 8: Create Contact Page CSS

```css
/* pages/Contact/index.module.css */
.pageWrapper {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.contentWrapper {
  flex: 1;
  padding: 40px 20px;
}

.contactContainer {
  max-width: 600px;
  margin: 0 auto;
}

.contactContainer h1 {
  font-size: 2.5em;
  margin-bottom: 10px;
  color: rgb(50, 50, 50);
  text-align: center;
}

.subtitle {
  font-size: 1.2em;
  color: rgb(100, 100, 100);
  text-align: center;
  margin-bottom: 40px;
}

.formWrapper {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.submitBtn {
  padding: 15px 30px;
  font-size: 1.2em;
  background-color: black;
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.submitBtn:hover {
  background-color: rgb(50, 50, 50);
  transform: scale(1.02);
}
```

## Step 9: Add Active Link Styling (Optional)

If you want to highlight the active page in navigation:

```javascript
// components/HomeHeader/index.jsx
import { Link, useLocation } from 'react-router-dom';
import styles from "./index.module.css";

function HomeHeader() {
  const location = useLocation();

  return (
    <header className={styles.headerContainer}>
        <Link to="/" className={styles.logoLink}>
          <h1 className={styles.logo}>BlogIt</h1>
        </Link>
        <nav className={styles.navGrp}>
          <ul>
            <li className={location.pathname === '/' ? styles.active : ''}>
              <Link to="/">Home</Link>
            </li>
            <li className={location.pathname === '/about' ? styles.active : ''}>
              <Link to="/about">About</Link>
            </li>
            <li className={location.pathname === '/contact' ? styles.active : ''}>
              <Link to="/contact">Contact Us</Link>
            </li>
          </ul>
        </nav>
    </header>
  );
}

export default HomeHeader;
```

Add this to HomeHeader CSS:

```css
.navGrp ul li.active {
  background-color: rgb(0, 0, 0);
  color: rgb(255, 220, 116);
}
```

## Folder Structure

```
src/
├── App.jsx
├── App.css
├── components/
│   ├── HomeHeader/
│   │   ├── index.jsx
│   │   └── index.module.css
│   ├── HomeFooter/
│   ├── PostList/
│   └── ... (other components)
├── pages/
│   ├── Home/
│   │   ├── index.jsx
│   │   └── index.module.css
│   ├── About/
│   │   ├── index.jsx
│   │   └── index.module.css
│   └── Contact/
│       ├── index.jsx
│       └── index.module.css
└── main.jsx
```


/////post routing
# Individual Post Routes Setup Guide

## Step 1: Update App.jsx with Route Parameters

```javascript
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';
import Contact from './pages/Contact';
import SinglePostPage from './pages/SinglePostPage';
import './App.css';

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/post/:postId" element={<SinglePostPage />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>
    </Router>
  );
}

export default App;
```

## Step 2: Create a Context for Sharing Posts Data

```javascript
// context/PostsContext.jsx
import { createContext, useState, useContext } from 'react';

const PostsContext = createContext();

export function PostsProvider({ children }) {
  const [posts, setPosts] = useState([
    {
      id: 1,
      title: "React Basics",
      author: "Jane Doe",
      date: "18-07-2024",
      content: "React is a popular JavaScript library developed by Facebook for building user interfaces, especially single-page applications (SPAs). It helps developers create fast and interactive web apps by breaking the UI into reusable components. Instead of reloading an entire page every time something changes, React updates only the parts of the page that need to change — making it efficient and responsive.\n\nThe core idea of React revolves around components. A component is a small, reusable piece of UI — for example, a button, header, or form. Components can be either class components or functional components, though functional components are now preferred with React Hooks. React uses a syntax called JSX (JavaScript XML), which allows you to write HTML-like code inside JavaScript. JSX makes it easier to visualize and structure UI elements directly within the logic.",
    },
    {
      id: 2,
      title: "State and Props",
      author: "John Smith",
      date: "19-07-2024",
      content: "React components often need to share or manage data. This is where props and state come in. Props (properties) are used to pass data from a parent component to a child component — similar to function parameters. State, on the other hand, represents data that belongs to a component itself and can change over time. When a component's state changes, React automatically re-renders that part of the UI to reflect the new data.",
    },
    {
      id: 3,
      title: "Lifecycle Methods",
      author: "Emily Johnson",
      date: "20-07-2024",
      content: "In React, lifecycle methods are special functions that run automatically at different stages of a component's life — from when it is created (mounted) to when it is removed (unmounted) from the UI. They allow developers to perform specific actions at these stages, such as fetching data, updating the DOM, or cleaning up resources. Lifecycle methods are mainly used in class components, while React Hooks serve a similar purpose in functional components.",
    },
  ]);

  const handleAddPost = (newPost) => {
    setPosts((prev) => [newPost, ...prev]);
  };

  const getPostById = (id) => {
    return posts.find(post => post.id === parseInt(id));
  };

  return (
    <PostsContext.Provider value={{ posts, handleAddPost, getPostById }}>
      {children}
    </PostsContext.Provider>
  );
}

export function usePosts() {
  const context = useContext(PostsContext);
  if (!context) {
    throw new Error('usePosts must be used within a PostsProvider');
  }
  return context;
}
```

## Step 3: Wrap App with PostsProvider

```javascript
// main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import { PostsProvider } from './context/PostsContext';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PostsProvider>
      <App />
    </PostsProvider>
  </React.StrictMode>,
);
```

## Step 4: Update Home Page to Remove PostList State

```javascript
// pages/Home/index.jsx
import HomeHeader from '../../components/HomeHeader';
import HomeFooter from '../../components/HomeFooter';
import PostList from '../../components/PostList';
import styles from './index.module.css';

function Home() {
  return (
    <div className={styles.pageWrapper}>
      <HomeHeader/>
      <div className={styles.contentWrapper}>
        <PostList/>
      </div>
      <HomeFooter/>
    </div>
  );
}

export default Home;
```

## Step 5: Update PostList Component

```javascript
// components/PostList/index.jsx
import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { usePosts } from "../../context/PostsContext";
import BlogHeader from "../BlogHeader";
import CreateBlogButton from "../CreateBlogButton";
import NewPostForm from "../NewPostForm";
import Modal from "../Modal";

function PostList() {
  const { posts, handleAddPost } = usePosts();
  const [isModalOpen, setIsModalOpen] = useState(false);
  const navigate = useNavigate();

  const handlePostAdded = (newPost) => {
    handleAddPost(newPost);
    navigate(`/post/${newPost.id}`);
  };

  return (
    <div>
      <section>
        <BlogHeader posts={posts} />
        <CreateBlogButton
          type="button"
          title="+ Create New Blog"
          onClick={() => setIsModalOpen(true)}
        />
        <Modal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)}>
          <NewPostForm
            onAddPost={handlePostAdded}
            onClose={() => setIsModalOpen(false)}
          />
        </Modal>
        
        <div style={{ 
          textAlign: 'center', 
          padding: '60px 20px',
          fontSize: '1.3em',
          color: '#666'
        }}>
          <p>👆 Click on any blog post card above to read the full article</p>
        </div>
      </section>
    </div>
  );
}

export default PostList;
```

## Step 6: Update BlogHeader to Navigate on Click

```javascript
// components/BlogHeader/index.jsx
import { useNavigate, useLocation } from "react-router-dom";
import styles from "./index.module.css";

function BlogHeader({ posts }) {
  const navigate = useNavigate();
  const location = useLocation();
  
  // Extract current post ID from URL
  const currentPostId = location.pathname.startsWith('/post/') 
    ? parseInt(location.pathname.split('/')[2]) 
    : null;

  return (
    <div className={styles.headerContainer}>
      <div className={styles.headerList}>
        {posts.map((post) => (
          <div 
            key={post.id}
            className={`${styles.postCard} ${currentPostId === post.id ? styles.active : ''}`}
            onClick={() => navigate(`/post/${post.id}`)}
          >
            <h3>{post.title}</h3>
            <h5>By: {post.author} <span>{post.date}</span></h5>
          </div>
        ))}
      </div>
    </div>
  );
}

export default BlogHeader;
```

## Step 7: Update BlogHeader CSS (Add Active State)

```css
/* components/BlogHeader/index.module.css */
.headerContainer{
    padding: 20px;
    background-color: rgb(225, 221, 214);
    overflow-x: auto;
    overflow-y: hidden;
}

.headerContainer::-webkit-scrollbar {
    height: 8px;
}

.headerContainer::-webkit-scrollbar-track {
    background: rgb(200, 196, 189);
    border-radius: 10px;
}

.headerContainer::-webkit-scrollbar-thumb {
    background: rgb(100, 100, 100);
    border-radius: 10px;
}

.headerContainer::-webkit-scrollbar-thumb:hover {
    background: rgb(50, 50, 50);
}

.headerList{
    display: flex;
    gap: 15px;
    width: max-content;
}

.postCard{
    min-width: 200px;
    font-size: 1.1em;
    padding: 10px 50px;
    border: 1px solid black;
    border-radius: 16px;
    cursor: pointer;
    transition: all 0.2s ease;
    flex-shrink: 0;
    background-color: white;
}

.postCard:hover{
    transform: scale(1.02);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.postCard.active {
    background-color: black;
    color: rgb(255, 220, 116);
    border: 2px solid black;
}

.postCard h3{
    text-decoration: underline;
}

.postCard h5{
    font-weight: 600;
}

.postCard span{
    font-weight: 300;
}
```

## Step 8: Create SinglePostPage Component

```javascript
// pages/SinglePostPage/index.jsx
import { useParams, useNavigate } from 'react-router-dom';
import { usePosts } from '../../context/PostsContext';
import HomeHeader from '../../components/HomeHeader';
import HomeFooter from '../../components/HomeFooter';
import BlogHeader from '../../components/BlogHeader';
import BlogPost from '../../components/BlogPost';
import styles from './index.module.css';

function SinglePostPage() {
  const { postId } = useParams();
  const { posts, getPostById } = usePosts();
  const navigate = useNavigate();
  const post = getPostById(postId);

  if (!post) {
    return (
      <div className={styles.pageWrapper}>
        <HomeHeader />
        <div className={styles.contentWrapper}>
          <div className={styles.notFound}>
            <h1>Post Not Found</h1>
            <p>The blog post you're looking for doesn't exist.</p>
            <button 
              className={styles.backBtn}
              onClick={() => navigate('/')}
            >
              Back to Home
            </button>
          </div>
        </div>
        <HomeFooter />
      </div>
    );
  }

  return (
    <div className={styles.pageWrapper}>
      <HomeHeader />
      <div className={styles.contentWrapper}>
        <BlogHeader posts={posts} />
        <BlogPost
          title={post.title}
          author={post.author}
          date={post.date}
          content={post.content}
        />
      </div>
      <HomeFooter />
    </div>
  );
}

export default SinglePostPage;
```

## Step 9: Create SinglePostPage CSS

```css
/* pages/SinglePostPage/index.module.css */
.pageWrapper {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.contentWrapper {
  flex: 1;
}

.notFound {
  text-align: center;
  padding: 80px 20px;
}

.notFound h1 {
  font-size: 3em;
  color: rgb(50, 50, 50);
  margin-bottom: 20px;
}

.notFound p {
  font-size: 1.3em;
  color: rgb(100, 100, 100);
  margin-bottom: 30px;
}

.backBtn {
  padding: 15px 30px;
  font-size: 1.2em;
  background-color: black;
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.backBtn:hover {
  background-color: rgb(50, 50, 50);
  transform: scale(1.05);
}
```

## Step 10: Update BlogPost Component (Keep as-is or enhance)

```javascript
// components/BlogPost/index.jsx
import { Component } from "react";
import styles from "./index.module.css";

class BlogPost extends Component {
  componentDidMount() {
    console.log(`BlogPost "${this.props.title}" has been mounted`);
    window.scrollTo(0, 0); // Scroll to top when post loads
  }

  componentDidUpdate(prevProps) {
    if (prevProps.title !== this.props.title) {
      console.log(
        `BlogPost has been updated from "${prevProps.title}" to "${this.props.title}"`
      );
      window.scrollTo(0, 0); // Scroll to top on post change
    }
  }

  render() {
    const { title, author, date, content } = this.props;
    const paragraphs = content.split('\n\n').filter(p => p.trim());

    return (
      <div className={styles.blogContainer}>
        <article className={styles.blogPostCard}>
          <h2>{title}</h2>
          <p>
            <strong>By:</strong> <big>{author}</big> | <i>{date}</i>
          </p>
          {paragraphs.map((para, idx) => (
            <p key={idx}>{para}</p>
          ))}
        </article>
      </div>
    );
  }
}

export default BlogPost;
```

## Final Folder Structure

```
src/
├── App.jsx
├── App.css
├── main.jsx
├── context/
│   └── PostsContext.jsx
├── components/
│   ├── HomeHeader/
│   ├── HomeFooter/
│   ├── BlogHeader/
│   ├── BlogPost/
│   ├── PostList/
│   ├── NewPostForm/
│   ├── Modal/
│   └── ... (other components)
├── pages/
│   ├── Home/
│   │   ├── index.jsx
│   │   └── index.module.css
│   ├── SinglePostPage/
│   │   ├── index.jsx
│   │   └── index.module.css
│   ├── About/
│   └── Contact/
```

